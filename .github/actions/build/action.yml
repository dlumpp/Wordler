name: 'Build'
description: 'Build .NET project'
runs:
  using: 'composite'
  steps:
    - name: Restore dependencies
      run: dotnet restore
      shell: bash
    - name: Build
      run: dotnet build --no-restore
      shell: bash
